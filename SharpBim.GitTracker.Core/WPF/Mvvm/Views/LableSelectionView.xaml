<uc:SharpBIMUserControl x:Class="SharpBIM.GitTracker.Core.WPF.Mvvm.Views.LableSelectionView"
                        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                        xmlns:Conv="clr-namespace:SharpBIM.WPF.Helpers.Converters;assembly=SharpBIM"
                        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                        xmlns:local="clr-namespace:SharpBIM.GitTracker.Core.WPF.Mvvm.Views"
                        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                        xmlns:uc="clr-namespace:SharpBIM.WPF.Controls.UserControls;assembly=SharpBIM"
                        xmlns:ui="clr-namespace:SharpBIM.WPF.Controls;assembly=SharpBIM"
                        d:DataContext="{d:DesignInstance Type={x:Type local:LabelsSelectionViewModel}}"
                        d:DesignHeight="450"
                        d:DesignWidth="800"
                        mc:Ignorable="d">
    <Grid>

        <Grid.Resources>
            <ResourceDictionary Source="/SharpBIM.GitTracker.Core;component/WPF/Mvvm/Views/DataTemplates.xaml" />
        </Grid.Resources>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="auto" />
        </Grid.RowDefinitions>
        <TextBlock FontSize="16"
                   FontWeight="Bold"
                   Text="Select Labels" />
        <ItemsControl Grid.Row="1"
                      ItemTemplate="{StaticResource LabelDataTemplate}"
                      ItemsSource="{Binding Children}">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel />
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
        </ItemsControl>
        <Grid Grid.Row="1"
              Background="Gray"
              Visibility="{Binding CreateNewLabel,
                                   Converter={Conv:ConvBoolToVisibility}}">
            <Grid HorizontalAlignment="Center"
                  VerticalAlignment="Center">
                <Border BorderBrush="White"
                        BorderThickness="5"
                        CornerRadius="5">
                    <StackPanel>
                        <uc:NameTextBoxComponent Header="Name"
                                                 TextValue="{Binding NewLabelName}" />
                        <uc:NameTextBoxComponent Header="Name"
                                                 TextValue="{Binding NewLabelColor}" />
                        <uc:NameTextBoxComponent Header="Name"
                                                 TextValue="{Binding NewLabelDescription}" />
                        <ui:UIButton Command="{Binding PushButtonCommand}" />
                    </StackPanel>
                </Border>
            </Grid>
        </Grid>
    </Grid>
</uc:SharpBIMUserControl>