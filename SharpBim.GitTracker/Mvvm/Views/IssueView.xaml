<uc:SharpBIMUserControl x:Class="SharpBIM.GitTracker.Mvvm.Views.IssueView"
                        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                        xmlns:av="http://icsharpcode.net/sharpdevelop/avalonedit"
                        xmlns:beh="clr-namespace:SharpBIM.GitTracker.Mvvm.Views.Behaviors"
                        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                        xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
                        xmlns:local="clr-namespace:SharpBIM.GitTracker.Mvvm.Views"
                        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                        xmlns:md="https://github.com/whistyun/MdXaml"
                        xmlns:mv="clr-namespace:SharpBIM.GitTracker.Mvvm.ViewModels"
                        xmlns:mvv="clr-namespace:SharpBim.GitTracker.Mvvm.Models"
                        xmlns:uc="clr-namespace:SharpBIM.WPF.Controls.UserControls;assembly=SharpBIM"
                        xmlns:ui="clr-namespace:SharpBIM.WPF.Controls;assembly=SharpBIM"
                        Margin="8"
                        HorizontalAlignment="Left"
                        d:DataContext="{d:DesignInstance Type={x:Type mv:IssueViewModel}}"
                        d:DesignHeight="450"
                        d:DesignWidth="800"
                        mc:Ignorable="d">
    <Grid>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid VerticalAlignment="Top">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <StackPanel Orientation="Horizontal">
                <ui:UIButton HorizontalAlignment="Left"
                             Command="{Binding PushIssueCommand}"
                             Content="Update"
                             DockPanel.Dock="Top"
                             ShowText="False" />
            </StackPanel>
        </Grid>
        <Grid Grid.Row="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="auto" />
                <RowDefinition Height="auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="5" />
                <RowDefinition Height="2*" />
            </Grid.RowDefinitions>
            <ui:TaggedTextBox Width="auto"
                              Height="auto"
                              HorizontalAlignment="Stretch"
                              Background="Transparent"
                              BorderThickness="0"
                              FontSize="18"
                              FontWeight="Bold"
                              Foreground="{DynamicResource ControlFontBrush}"
                              Text="{Binding Title}" />
            <Grid Grid.Row="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="auto" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <ui:UIButton Command="{Binding AddNewLableCommand}"
                             ShowText="False" />
                <ItemsControl Grid.Column="1"
                              ItemsSource="{Binding IssueLables}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel />
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate DataType="{x:Type mvv:LabelModelView}">
                            <ui:UIButton Width="auto"
                                         Margin="2"
                                         Background="{Binding BackgroundBrush}"
                                         Command="{Binding AddRemoveCommand}"
                                         Content="{Binding Title}"
                                         Foreground="{Binding FontBrush}"
                                         RadiusThickness="15"
                                         Style="{StaticResource RoundedButtonStyle}"
                                         ToolTip="{Binding Hint}" />
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </Grid>
            <av:TextEditor x:Name="avtxt"
                           Grid.Row="2"
                           MinHeight="200"
                           Margin="5"
                           Background="{DynamicResource ControlBackgroundBrush}"
                           Foreground="{DynamicResource ControlForegroundBrush}"
                           PreviewKeyDown="Avtxt_PreviewKeyDown"
                           ScrollViewer.VerticalScrollBarVisibility="Auto"
                           WordWrap="True">
                <i:Interaction.Behaviors>
                    <beh:AvalonEditBehaviour GiveMeTheText="{Binding MarkDown, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                </i:Interaction.Behaviors>
            </av:TextEditor>
            <GridSplitter Grid.Row="3"
                          HorizontalAlignment="Stretch"
                          ResizeBehavior="PreviousAndNext"
                          ResizeDirection="Rows" />
            <md:MarkdownScrollViewer x:Name="mk"
                                     Grid.Row="4"
                                     Margin="5"
                                     Background="{DynamicResource ControlBackgroundBrush}"
                                     Foreground="{DynamicResource ControlForegroundBrush}"
                                     Markdown="{Binding MarkDown,
                                                        Mode=OneWay,
                                                        UpdateSourceTrigger=PropertyChanged}"
                                     ScrollViewer.VerticalScrollBarVisibility="Auto" />
        </Grid>
    </Grid>
</uc:SharpBIMUserControl>